name: Container1
on: push

jobs: 
      node-docker:
        runs-on: ubuntu-latest
        container: 
           image: node
           ports: 
             - 4001:4000
        steps:
           - name: Log node version
             run: |
                node -v
                cat /etc/os-release