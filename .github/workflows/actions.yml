name: Actions workflow

on:
  schedule:
    - cron: "0/5 * * * *"

  push:

  pull_request:
    types: [closed, assigned, opened, reopened]

jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    steps:
      - name: List files
        run: |
          pwd
          ls -a
          touch harshita004.txt